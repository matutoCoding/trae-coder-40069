## 1. 架构设计

```mermaid
flowchart LR
    subgraph "前端层"
        A["React 18 单页应用"]
        B["React Router 路由"]
        C["状态管理 (Zustand)"]
        D["UI组件库 (Ant Design)"]
        E["图表组件 (ECharts)"]
        F["Mock数据服务"]
    end

    subgraph "数据层"
        G["本地Mock数据"]
        H["实时数据模拟"]
    end

    A --> B
    A --> C
    A --> D
    A --> E
    C --> F
    F --> G
    F --> H

    style A fill:#165DFF,color:#fff
    style D fill:#165DFF,color:#fff
    style E fill:#165DFF,color:#fff
    style G fill:#00B42A,color:#fff
    style H fill:#FF7D00,color:#fff
```

## 2. 技术描述

- **前端框架**：React@18 + TypeScript
- **构建工具**：Vite@5
- **样式方案**：TailwindCSS@3 + 自定义CSS变量
- **UI组件库**：Ant Design@5
- **图表组件**：ECharts@5
- **路由管理**：React Router@6
- **状态管理**：Zustand@4
- **数据方案**：本地Mock数据，模拟实时数据更新
- **图标库**：Lucide React

## 3. 路由定义

| 路由路径 | 页面名称 | 模块归属 |
|----------|----------|----------|
| `/` | 首页仪表盘 | 总览 |
| `/startup-shutdown` | 装置开停工 | 装置开停工 |
| `/catalyst` | 催化剂管理 | 催化剂管理 |
| `/reaction-regeneration` | 反应再生 | 反应再生 |
| `/fractionation` | 分馏吸收 | 分馏吸收 |
| `/energy-recovery` | 能量回收 | 能量回收 |
| `/monitoring` | 参数监控 | 参数监控 |
| `/equipment` | 设备点检 | 设备点检 |

## 4. 数据模型

### 4.1 数据模型定义

```mermaid
erDiagram
    UNIT ||--o{ SHUTDOWN_PLAN : has
    UNIT ||--o{ CATALYST : contains
    UNIT ||--o{ REACTOR_PARAM : monitors
    UNIT ||--o{ FRACTIONATOR_PARAM : monitors
    UNIT ||--o{ ENERGY_DATA : produces
    UNIT ||--o{ EQUIPMENT : has
    EQUIPMENT ||--o{ INSPECTION : has
    CATALYST ||--o{ CATALYST_LOSS : records
    PARAMETER ||--o{ ALARM : triggers

    UNIT {
        string id PK
        string name
        string status
        string type
    }

    SHUTDOWN_PLAN {
        string id PK
        string unitId FK
        string name
        string type
        string status
        datetime startTime
        datetime endTime
        json steps
    }

    CATALYST {
        string id PK
        string unitId FK
        string name
        float inventory
        float inventoryUnit
        datetime lastReplaced
    }

    CATALYST_LOSS {
        string id PK
        string catalystId FK
        float lossAmount
        datetime recordTime
        string reason
    }

    REACTOR_PARAM {
        string id PK
        string unitId FK
        float temperature
        float pressure
        float catalystCirculation
        datetime recordTime
    }

    FRACTIONATOR_PARAM {
        string id PK
        string unitId FK
        float topTemperature
        float bottomTemperature
        float slurryFlow
        datetime recordTime
    }

    ENERGY_DATA {
        string id PK
        string unitId FK
        float flueGasFlow
        float steamProduction
        float recoveryEfficiency
        datetime recordTime
    }

    EQUIPMENT {
        string id PK
        string unitId FK
        string name
        string type
        string status
        datetime lastMaintenance
    }

    INSPECTION {
        string id PK
        string equipmentId FK
        string inspector
        datetime inspectionTime
        string status
        string remarks
    }

    PARAMETER {
        string id PK
        string name
        float upperLimit
        float lowerLimit
        string unit
    }

    ALARM {
        string id PK
        string parameterId FK
        float actualValue
        string level
        datetime alarmTime
        string status
    }
```

### 4.2 目录结构

```
src/
├── assets/              # 静态资源
├── components/          # 公共组件
│   ├── Layout/          # 布局组件
│   ├── Dashboard/       # 仪表盘组件
│   ├── Charts/          # 图表组件
│   └── Common/          # 通用组件
├── pages/               # 页面组件
│   ├── Dashboard/       # 首页仪表盘
│   ├── StartupShutdown/ # 装置开停工
│   ├── Catalyst/        # 催化剂管理
│   ├── Reaction/        # 反应再生
│   ├── Fractionation/   # 分馏吸收
│   ├── EnergyRecovery/  # 能量回收
│   ├── Monitoring/      # 参数监控
│   └── Equipment/       # 设备点检
├── store/               # 状态管理
├── mock/                # Mock数据
├── types/               # TypeScript类型定义
├── utils/               # 工具函数
├── App.tsx
├── main.tsx
└── index.css
```

## 5. 核心技术决策

### 5.1 实时数据模拟

- 使用 `setInterval` 模拟实时数据更新，每2-5秒随机更新关键参数
- 数据波动范围符合实际炼油工艺参数范围
- 报警状态根据随机生成的参数值与阈值比较自动触发

### 5.2 图表实现

- 使用 ECharts 实现所有数据可视化
- 折线图：参数趋势分析、催化剂跑损趋势
- 柱状图：能耗统计、催化剂对比
- 仪表盘：关键指标实时显示（温度、压力、效率）
- 热力图：反应器温度分布

### 5.3 样式方案

- TailwindCSS 实现快速布局
- 自定义CSS变量定义工业风格主题色
- 深色主题为主，配合数据发光效果
- 响应式断点：1920px、1024px、768px

### 5.4 状态管理

- Zustand 轻量级状态管理
- 全局状态存储：用户信息、报警信息、实时参数
- 页面级状态通过组件内部 `useState` 管理

### 5.5 Mock数据规范

- 所有数据符合炼油厂催化裂化装置实际参数范围
- 温度范围：450-750°C
- 压力范围：0.1-0.4 MPa
- 催化剂藏量：100-200 吨
- 数据生成带有合理的随机波动，模拟真实生产环境
